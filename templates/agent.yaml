name: {{WORKSPACE_NAME}}
version: 0.1.0
description: "AI workspace powered by Multi-AI Platform"

models:
  primary: claude-opus-4-6
  fallback:
    - gemini-3.1-pro
    - gpt-5.3

memory:
  sessions: sessions/
  wiki: wiki/

hooks:
  session_start: "map hook start {client}"
  session_end: "map hook end {client}"

skills:
  - council

compliance:
  session_retention: permanent
  wiki_retention: permanent
